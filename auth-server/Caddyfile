{$AUTH_DOMAIN} {
  encode zstd gzip
  @server path /v1/server/*
  respond @server 404
  reverse_proxy api:8787

  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains"
    X-Content-Type-Options "nosniff"
    Referrer-Policy "no-referrer"
  }
}
